[tool.poetry]
name = "repl_python3_discord-study-bot-1"
version = "0.1.0"
description = ""
authors = ["JeffreyLin21 <<>>"]

[tool.poetry.dependencies]
python = "^3.8"
discord = "^1.0.1"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
